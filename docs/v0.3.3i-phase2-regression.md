# V0.3.3i UI 收口二阶段回归

日期：2026-08-02

## 二阶段推进补记

- 已将 `styles.css` 中被 `@media not all` 隔离的历史 UI 补丁物理删除，文件从 5141 行缩减到 1764 行。
- `styles.css` 现在只保留基础样式，运行态 UI 权威继续由 `styles-ui.css` 与 `styles-mobile.css` 接管。
- `tools/audit-style-systems.mjs` 已从“检查历史补丁隔离存在”改为“检查历史补丁不存在”，避免旧 CSS 再混入运行态。
- 删除后重新跑过样式、资源、安全区、桌面可视和竖屏可视回归，均通过。

## 结论

当前 V0.3.3i 二阶段收口后的运行态基本稳定。正式 UI 权威文件已生效，旧 CSS 补丁已被隔离，资源和安全区审计未发现阻断问题。本地预览地址可访问。

## 已通过检查

- `tools/audit-style-systems.mjs`：通过。确认 `styles.css` 历史 UI 补丁隔离，`styles-ui.css` 桌面正式 UI 生效，`styles-mobile.css` 竖屏正式 UI 生效。
- `tools/audit-assets.mjs`：通过。运行态 manifest 版本为 `0.3.3l-client-opt`，73 个声明资源均存在，无原型资源引用。
- `tools/audit-ui-safe-areas.mjs`：通过，`issueCount: 0`。
- `node --check src/game.js`：通过。
- `tools/visual-run.mjs`：通过。桌面开局、升级、剧情、暂停、结算、压力场景无 JS 错误。
- `tools/visual-mobile.mjs`：通过。竖屏移动 HUD 生效，桌面 HUD 隐藏，开局确认按钮可见。
- 本地预览：`http://127.0.0.1:4177/index.html` 返回 200。

## 视觉回归观察

- 桌面升级卡牌：卡组、标题、跳过按钮整体已居中，较上一轮明显稳定。
- 桌面剧情框：标题、头像、正文和按钮已基本在安全区内，叙事 UI 可以继续作为当前正式方向。
- 竖屏开局：底部确认按钮已经明确显示，解决“选中后不知道如何确定”的问题。
- 竖屏升级：当前可操作，但升级卡牌占屏仍偏大，右上资源框仍像桌面资源框缩放适配，建议归入 V0.3.5 移动端专项。
- HUD：左上信息面板仍偏小、偏挤，文本层级不够清爽；这是视觉细修项，不是阻断问题。

## 物理删除旧 CSS 后的实机截图复查

- 桌面升级卡：技能图标基本压在顶部圆章中心，卡牌标题、正文和按钮均在纸面安全区内，三卡组和标题居中。仍需注意升级暂停时战斗飘字会透到标题附近，后续可加强遮罩或冻结特效层。
- 桌面开局卡：人物图、名称、功法名和数值均在卡面内；开局选中感仍偏弱，后续应补正式选中印章/光框图件，而不是靠 CSS 外框。
- 桌面剧情框：剧情标题已回到标题牌内，正文和按钮在安全区内；标题牌略偏左但不再是严重错位。
- 竖屏开局：底部确认按钮明确，文字不出框；但仍是桌面大卡横滑适配，后续 V0.3.5 应单独做移动端小卡 atlas。
- 竖屏升级：可操作，文字在卡内；但卡牌占屏过大、右上资源框仍像桌面资源框硬适配，继续列为移动端专项。

## 未通过 / 阻断

- `build-site.js` 仍因 `deploy-root` 目录 EPERM 无法删除而失败。该问题与本轮 UI 收口无直接关系，判断为本地构建目录被占用或权限锁定。发布前需要释放或重建 `deploy-root`。

## 下一步建议

1. 先保留当前二阶段收口状态，继续用截图回归观察 1-2 轮。
2. 如果视觉稳定，再物理删除或归档 `styles.css` 中被 `@media not all` 隔离的历史 UI 补丁。
3. 进入 V0.3.3j 轩辕样板收尾：补地表细节、低矮摆件、事件点位反馈。
4. 将竖屏开局/升级/资源框列入 V0.3.5 手机端专属 UI atlas，不再继续用桌面 UI 强行缩放。
