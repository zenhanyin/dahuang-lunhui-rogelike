# V0.3.2e UI Style Close And V0.3.3 Map Polish

## Done

- Bumped runtime cache version to `0.3.2e-ui-style-close`.
- Added a final formal UI override layer in `styles.css` for:
  - HUD
  - resource counters
  - pause button
  - level badge
  - dash button
  - start lineage cards
  - upgrade cards
  - story dialogue
  - pause panel
  - result panel
  - current build panel
  - mobile HUD overrides
- Unified visible UI image query versions to `0.3.2e-ui-style-close`.
- Removed old scene pack random weights for Xuanyuan and Shennong variants.
- Regenerated Xuanyuan and Shennong map atlas with quieter base tile texture.

## Regression

- `tools/smoke-test.mjs`: pass.
- `tools/audit-style-systems.mjs`: pass.
- `tools/visual-run.mjs`: pass.
- `tools/visual-scene-packs-v032d.mjs`: pass.
- `tools/visual-mobile.mjs`: pass.
- `tools/stress-probe.mjs`: pass, around 4.17 ms/frame with 60 enemies in the latest probe.

## Current Visual Assessment

- UI is now more unified than V0.3.2d. Upgrade cards and start cards are the strongest formal UI pieces.
- HUD/resource/pause/result/build are using formal atlas assets, but some content layout still depends on HTML text grids. Further improvement should be an atlas-safe layout pass, not more small CSS patches.
- Qingqiu remains the best scene template.
- Xuanyuan and Shennong now run through the same scene-pack pipeline and are less blocky than the first V0.3.3 attempt, but they are still template-grade. Their tile repetition is visible because the current generator intentionally keeps the base quiet and reusable.

## Next

1. Formal UI content grid pass:
   - Define safe text rectangles for every panel asset.
   - Remove historical duplicate CSS blocks once the final rule set is stable.
   - Add visual audit thresholds for title/button/card bounds.
2. Formal map art pass:
   - Produce 2-3 hand-painted or image-generated seamless base tiles per map.
   - Keep base tiles low contrast, no center light, no big edge-cut motifs.
   - Use decals/props/events for scene identity.
3. Build perception pass:
   - Improve current build UI and in-run upgrade feedback.
   - Show owned skill/passive levels and evolution route more clearly.
