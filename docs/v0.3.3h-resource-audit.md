# V0.3.3h Resource Inventory Audit

Date: 2026-07-31

## Current Runtime State

- Runtime hook: `0.3.3k-mobile-start-confirm`
- Static code/CSS asset references: `135`
- Global runtime manifest entries: `73`
- Global runtime manifest size: `794341` bytes
- Code reference scope: `src/game.js`, `src/config.js`, `styles.css`, `index.html`

## Asset Buckets

- `assets/runtime/webp`: formal runtime characters, enemies, UI, VFX.
- `assets/maps/v032_atlas`: accepted Qingqiu runtime sample plus legacy Shennong validation assets.
- `assets/maps/v033`: Xuanyuan formal map pack and v033 map templates.
- `assets/art_direction`: source references and accepted direction images, not shipped.
- `assets/concepts`: concept/keyframe images, not shipped.
- `assets/generated`: temporary/generated source outputs, not shipped.
- `assets/sprites` and `assets/ui`: deprecated prototype roots, not shipped.

## Map Pack Status

- Qingqiu: accepted runtime sample, still under `assets/maps/v032_atlas/qingqiu*`.
- Xuanyuan: formal v033 pack, `assets/maps/v033/xuanyuan_ground/manifest.json`, seam-reviewed base tiles enabled.
- Shennong: legacy validation pack only; formal v033 Shennong pack is not ready.

## Cleanup Policy

- Hard delete only files that are proven unreferenced and not useful as source material.
- Source-only folders are excluded by `.gitignore` and `build-site.js`, so they do not enter the shipped build.
- Deprecated prototype paths are audited as forbidden runtime references:
  - `assets/sprites/`
  - `assets/ui/`
  - `assets/generated/`
  - `assets/runtime/raw_ai_atlas/`
  - `assets/runtime/webp/ui/story_raw/`
  - old `assets/maps/v032/sword_tomb|qingqiu|herb_marsh/`

## Required Checks

Run before deployment:

```bash
npm run audit:assets
npm run audit:style
node --check src/game.js
```

The asset audit verifies current manifest version, manifest byte totals, static asset references, map-pack manifest paths, and forbidden prototype references.
